module.exports = {
    button: {
        id: '', // Needs to be this ID
        contains: false // Allow if the button's id contains this over ^^
    },
    execute: async(interaction, bot) => {} // On the button execute, if the "button" is matching
}